VITE_HCAPTCHA_SITE_KEY=
VITE_API_BASE_URL=https://api.example.com

# Dev-only, for the /api/dashboard Vite proxy (mirrors nginx.conf in prod).
# Deliberately NOT VITE_-prefixed: must never be inlined into the client bundle.
# Put real values in a gitignored .env.local, not here.
HOMELAB_API_ORIGIN=http://localhost:8080
HOMELAB_API_TOKEN=
